{
  "id": "openguardrails",
  "name": "OpenGuardrails",
  "description": "Behavioral anomaly detection for OpenClaw agents.",
  "version": "6.0.3",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "description": "Enable or disable the guard (default: true)"
      },
      "blockOnRisk": {
        "type": "boolean",
        "description": "Block tool calls when behavioral risk is detected (default: true)"
      },
      "apiKey": {
        "type": "string",
        "description": "API key (optional — auto-registered if missing)"
      },
      "timeoutMs": {
        "type": "number",
        "description": "Timeout for cloud assessment in milliseconds (default: 60000)"
      },
      "agentName": {
        "type": "string",
        "description": "Agent name used during registration (default: OpenClaw Agent)"
      },
      "platformUrl": {
        "type": "string",
        "description": "Platform URL for core API — registration and behavioral detection (default: https://platform.openguardrails.com)"
      },
      "apiBaseUrl": {
        "type": "string",
        "description": "Deprecated — use platformUrl"
      }
    }
  },
  "uiHints": {
    "enabled": {
      "label": "Enable Guard",
      "help": "Enable or disable behavioral anomaly detection"
    },
    "blockOnRisk": {
      "label": "Block on Risk",
      "help": "Block tool calls when behavioral risk is detected"
    },
    "apiKey": {
      "label": "API Key",
      "help": "Your API key. Leave blank for automatic registration on first use.",
      "advanced": true
    },
    "timeoutMs": {
      "label": "Timeout (ms)",
      "help": "Maximum time for cloud assessment",
      "advanced": true
    },
    "agentName": {
      "label": "Agent Name",
      "help": "Name shown in the OpenGuardrails dashboard for this agent.",
      "advanced": true
    },
    "platformUrl": {
      "label": "Platform URL",
      "help": "Override the core API endpoint — useful for local development (e.g. http://localhost:3002).",
      "advanced": true
    },
    "apiBaseUrl": {
      "label": "API Base URL (deprecated)",
      "help": "Deprecated — use platformUrl instead.",
      "advanced": true
    }
  }
}
