# Sandbox integrations

Sandbox integrations bind OGR policy to the boundary that actually executes a
process or mediates filesystem and network access.

Planned standalone examples:

- Anthropic Sandbox Runtime (`srt`)
- NVIDIA OpenShell

The Hermes agent integration currently demonstrates both backends end to end
under [`../agent/hermes/`](../agent/hermes/).
