#!/usr/bin/env node
// GENERATED by 'npm run build' from hooks/ogr-hook.src.mjs — do not edit by hand.

// hooks/ogr-hook.src.mjs
import { readFileSync } from "node:fs";
import { fileURLToPath } from "node:url";
import { dirname, join } from "node:path";

// ../../packages/javascript/dist/models.js
var OGR_VERSION = "0.1";
var DECISIONS = ["block", "require_approval", "redact", "modify", "allow"];
function severity(decision) {
  return DECISIONS.indexOf(decision);
}

// ../../packages/javascript/dist/composition.js
var COMPOSED_PROVIDER = "ogr.runtime/composed";
function merge(ev, decision, verdicts, reasonPrefix) {
  const cats = /* @__PURE__ */ new Map();
  const reasons = [];
  const evidence = [];
  for (const v of verdicts) {
    for (const c of v.categories) {
      const existing = cats.get(c.id);
      if (!existing || c.score > existing.score)
        cats.set(c.id, c);
    }
    for (const r of v.reasons)
      reasons.push(`[${v.provider}] ${r}`);
    evidence.push({ provider: v.provider, decision: v.decision, latencyMs: v.latencyMs });
  }
  return {
    eventId: ev.eventId,
    guardId: ev.guardId,
    provider: COMPOSED_PROVIDER,
    decision,
    categories: [...cats.values()],
    reasons: [reasonPrefix, ...reasons],
    evidence,
    ogrVersion: OGR_VERSION
  };
}
var mostSevere = (verdicts) => verdicts.reduce((a, b) => severity(b.decision) < severity(a.decision) ? b : a);
function compose(ev, verdicts, rule) {
  const strategy = rule.strategy ?? "deny-wins";
  if (verdicts.length === 0) {
    return {
      eventId: ev.eventId,
      guardId: ev.guardId,
      provider: COMPOSED_PROVIDER,
      decision: rule.on_all_failed ?? "allow",
      categories: [],
      reasons: ["no detector produced a verdict"],
      ogrVersion: OGR_VERSION
    };
  }
  if (strategy === "deny-wins") {
    const winner2 = mostSevere(verdicts);
    return merge(ev, winner2.decision, verdicts, `deny-wins \u2192 ${winner2.decision}`);
  }
  if (strategy === "quorum") {
    const q = rule.quorum ?? { count: 2, min_score: 0 };
    const minScore = q.min_score ?? 0;
    const votes = verdicts.filter((v) => v.decision !== "allow" && (v.categories.some((c) => c.score >= minScore) || v.categories.length === 0));
    if (votes.length >= (q.count ?? 2)) {
      const winner2 = mostSevere(votes);
      return merge(ev, winner2.decision, verdicts, `quorum ${votes.length}/${q.count} \u2192 ${winner2.decision}`);
    }
    return merge(ev, "allow", verdicts, "quorum not reached \u2192 allow");
  }
  if (strategy === "first-available") {
    return merge(ev, verdicts[0].decision, verdicts, "first-available");
  }
  const winner = mostSevere(verdicts);
  return merge(ev, winner.decision, verdicts, `default most_severe \u2192 ${winner.decision}`);
}
function selectRule(verdicts, composition) {
  const flagged = /* @__PURE__ */ new Set();
  for (const v of verdicts)
    for (const c of v.categories)
      flagged.add(c.id);
  let best = composition["default"] ?? { strategy: "deny-wins" };
  let bestLen = -1;
  for (const [prefix, rule] of Object.entries(composition)) {
    if (prefix === "default" || prefix === "conflict_default")
      continue;
    const base = prefix.replace(/\*+$/, "").replace(/\.+$/, "");
    const matches = [...flagged].some((cid) => cid === base || cid.startsWith(base + ".") || base === "");
    if (matches && base.length > bestLen) {
      best = rule;
      bestLen = base.length;
    }
  }
  return best;
}

// ../../packages/javascript/dist/detectors/index.js
function appliesTo(detector, ev) {
  if (detector.appliesTo)
    return detector.appliesTo(ev);
  return !detector.handles || detector.handles.length === 0 || detector.handles.includes(ev.kind);
}

// ../../packages/javascript/dist/runtime.js
var Runtime = class {
  detectors;
  composition;
  events = /* @__PURE__ */ new Map();
  // eventId -> event
  byGuard = /* @__PURE__ */ new Map();
  // guardId -> effective verdict so far
  constructor(detectors, policy) {
    this.detectors = detectors;
    this.composition = policy.composition ?? {};
  }
  /** Inherit provenance from referenced prior events. */
  enrich(ev) {
    for (const ref of ev.contextRefs ?? []) {
      const prior = this.events.get(ref);
      if (prior)
        ev.provenance.push(...prior.provenance);
    }
    return ev;
  }
  async evaluate(ev) {
    this.enrich(ev);
    this.events.set(ev.eventId, ev);
    const applicable = this.detectors.filter((d) => appliesTo(d, ev));
    const verdicts = await Promise.all(applicable.map((d) => Promise.resolve(d.evaluate(ev))));
    const rule = selectRule(verdicts, this.composition);
    const effective = compose(ev, verdicts, rule);
    const prior = this.byGuard.get(ev.guardId);
    if (prior && severity(prior.decision) < severity(effective.decision)) {
      effective.decision = prior.decision;
      effective.reasons.push(`[correlation] tightened to prior decision '${prior.decision}' from earlier observation point`);
    }
    this.byGuard.set(ev.guardId, effective);
    return effective;
  }
};

// ../../packages/javascript/dist/detectors/config-rules.js
var SHELL_TOOLS = /* @__PURE__ */ new Set([
  "shell.exec",
  "bash",
  "run_shell",
  "terminal",
  "run_terminal_cmd",
  "execute_code",
  "run_code"
]);
function commandString(ev) {
  if (ev.kind === "exec") {
    const argv = ev.payload["argv"] ?? [];
    return argv.join(" ");
  }
  if (ev.kind === "tool_call" && SHELL_TOOLS.has(String(ev.payload["name"] ?? ""))) {
    const args = ev.payload["arguments"] ?? {};
    return args["cmd"] ?? args["command"] ?? args["code"];
  }
  return void 0;
}
var maxDecision = (a, b) => severity(a) <= severity(b) ? a : b;
var ConfigRulesDetector = class {
  cfg;
  provider = "ogr.config_rules";
  handles = ["exec", "tool_call", "network"];
  patterns;
  constructor(cfg) {
    this.cfg = cfg;
    this.patterns = (cfg.command_rules ?? []).map((r) => [new RegExp(r.regex), r]);
  }
  evaluate(ev) {
    const t0 = Date.now();
    const cats = [];
    const reasons = [];
    let decision = "allow";
    if (ev.kind === "network") {
      const host = String(ev.payload["host"] ?? "");
      const allow = this.cfg.egress_allowlist ?? [];
      if (allow.length > 0 && !allow.includes(host)) {
        decision = "block";
        cats.push({ id: "security.ssrf", domain: "security", score: 1 });
        reasons.push(`egress to '${host}' not in allow-list ${JSON.stringify(allow)}`);
      }
    }
    const cmd = commandString(ev);
    if (cmd) {
      for (const [rx, rule] of this.patterns) {
        if (rx.test(cmd)) {
          decision = maxDecision(decision, rule.decision ?? "block");
          cats.push({ id: rule.category, domain: rule.domain ?? "security", score: rule.score ?? 1 });
          reasons.push(`matched rule '${rule.id}': ${rule.why}`);
        }
      }
      const markers = this.cfg.secret_env_markers ?? [];
      const envKeys = ev.payload["env_keys"] ?? [];
      const secretEnv = envKeys.filter((k) => markers.some((s) => k.toUpperCase().includes(s)));
      if (secretEnv.length > 0) {
        decision = maxDecision(decision, "require_approval");
        cats.push({ id: "security.secret_leak", domain: "security", score: 0.8 });
        reasons.push(`secrets exposed to process env: ${JSON.stringify(secretEnv)}`);
      }
    }
    return {
      eventId: ev.eventId,
      guardId: ev.guardId,
      provider: this.provider,
      decision,
      categories: cats,
      reasons: reasons.length ? reasons : ["no rule matched"],
      latencyMs: Date.now() - t0,
      ogrVersion: OGR_VERSION
    };
  }
};

// hooks/ogr-hook.src.mjs
var HERE = dirname(fileURLToPath(import.meta.url));
function readStdin() {
  try {
    return readFileSync(0, "utf8");
  } catch {
    return "";
  }
}
function loadPolicy() {
  const path = process.env.OGR_POLICY || join(HERE, "..", "policy", "policy.json");
  return JSON.parse(readFileSync(path, "utf8"));
}
var seq = 0;
var id = (p) => `${p}_${Date.now().toString(36)}_${(seq++).toString(36)}`;
function commandOf(input) {
  const tool = input.tool_name ?? input.tool ?? "";
  const ti = input.tool_input ?? input.toolInput ?? {};
  if (tool === "Bash") return [tool, ti.command];
  if (tool === "WebFetch") return [tool, ti.url];
  if (tool === "Read" || tool === "Edit" || tool === "Write") return [tool, ti.file_path ?? ti.path];
  return [tool, JSON.stringify(ti ?? {})];
}
function buildGuardEvent(tool, command) {
  const guardId = id("g");
  return {
    kind: "exec",
    observationPoint: "agent_hook",
    subject: { tool },
    payload: { argv: [String(command ?? "")], tool, name: tool },
    eventId: id("e"),
    guardId,
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    provenance: [{ source: "model", trust: "unverified" }],
    ogrVersion: OGR_VERSION
  };
}
function hostsIn(text) {
  const out = [];
  const re = /https?:\/\/([^/\s"'`)]+)/gi;
  let m;
  while (m = re.exec(text)) out.push(m[1].replace(/^.*@/, "").split(":")[0].toLowerCase());
  return out;
}
var hostAllowed = (host, allow) => allow.some((p) => {
  const pat = p.toLowerCase();
  return pat.startsWith("*.") ? host === pat.slice(2) || host.endsWith(pat.slice(1)) : host === pat;
});
var CommandEgressSecretsDetector = class {
  constructor(policy) {
    this.policy = policy;
    this.provider = "ogr.command_egress_secrets";
    this.handles = ["exec"];
  }
  evaluate(ev) {
    const cmd = (ev.payload.argv ?? []).join(" ") || "";
    const reasons = [];
    const categories = [];
    let decision = "allow";
    const bump = (d) => {
      const order = ["block", "require_approval", "redact", "modify", "allow"];
      if (order.indexOf(d) < order.indexOf(decision)) decision = d;
    };
    const allow = this.policy.egress_allowlist ?? [];
    if (allow.length) {
      for (const host of hostsIn(cmd)) {
        if (!hostAllowed(host, allow)) {
          bump("require_approval");
          categories.push({ id: "security.egress", domain: "security", score: 0.7 });
          reasons.push(`egress to '${host}' is not in the OGR allow-list`);
        }
      }
    }
    for (const marker of this.policy.secret_read_markers ?? []) {
      if (cmd.includes(marker)) {
        bump("require_approval");
        categories.push({ id: "security.secret_leak", domain: "security", score: 0.8 });
        reasons.push(`references a credential-bearing path ('${marker}')`);
        break;
      }
    }
    return {
      eventId: ev.eventId,
      guardId: ev.guardId,
      provider: this.provider,
      decision,
      categories,
      reasons: reasons.length ? reasons : ["no egress/secret finding"],
      ogrVersion: OGR_VERSION
    };
  }
};
function toClaudeDecision(d) {
  if (d === "block") return "deny";
  if (d === "allow") return "allow";
  return "ask";
}
function emit(permissionDecision, reason) {
  if (permissionDecision === "allow") process.exit(0);
  process.stdout.write(
    JSON.stringify({
      hookSpecificOutput: {
        hookEventName: "PreToolUse",
        permissionDecision,
        permissionDecisionReason: `[OpenGuardrails] ${reason}`
      }
    })
  );
  process.exit(0);
}
async function main() {
  let input;
  try {
    input = JSON.parse(readStdin() || "{}");
  } catch (e) {
    process.stderr.write(`[OpenGuardrails] could not parse hook input, allowing: ${e}
`);
    process.exit(0);
  }
  try {
    const policy = loadPolicy();
    const [tool, command] = commandOf(input);
    const ev = buildGuardEvent(tool, command);
    const runtime = new Runtime(
      [new ConfigRulesDetector(policy), new CommandEgressSecretsDetector(policy)],
      { composition: policy.composition ?? { "security.*": { strategy: "deny-wins" }, default: { strategy: "deny-wins" } } }
    );
    const verdict = await runtime.evaluate(ev);
    const reason = (verdict.reasons ?? []).filter((r) => !/no .* finding|no rule matched/.test(r)).join("; ") || "policy violation";
    emit(toClaudeDecision(verdict.decision), reason);
  } catch (e) {
    process.stderr.write(`[OpenGuardrails] hook error, allowing this call: ${e}
`);
    process.exit(0);
  }
}
main();
